# RAG Chat — LangGraph

```mermaid
---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	summarize_memory(summarize_memory)
	analyze_question(analyze_question)
	rewrite_question(rewrite_question)
	retrieve(retrieve)
	generate_rag(generate_rag)
	no_context_answer(no_context_answer)
	generate_direct(generate_direct)
	generate_chitchat(generate_chitchat)
	__end__([<p>__end__</p>]):::last
	__start__ --> summarize_memory;
	analyze_question -. &nbsp;chitchat&nbsp; .-> generate_chitchat;
	analyze_question -. &nbsp;chess&nbsp; .-> generate_direct;
	analyze_question -. &nbsp;system&nbsp; .-> rewrite_question;
	retrieve -.-> generate_rag;
	retrieve -.-> no_context_answer;
	rewrite_question --> retrieve;
	summarize_memory --> analyze_question;
	generate_chitchat --> __end__;
	generate_direct --> __end__;
	generate_rag --> __end__;
	no_context_answer --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc

```
