Classify the user's question into question_type and domain.

1. "question_type":
- "rag": Needs a verified source of truth — official FIDE rules, piece movements, special moves, tournament procedures, platform workflows (rooms, matchmaking, reconnects, account settings, forum rules, error handling), or factual information.
- "general": Casual greetings, compliments, small talk, subjective opinions without need for factual retrieval (e.g. "hello", "how are you", "what opening do you like?").

2. "domain":
- "chess": Specifically about chess rules, FIDE regulations, piece movements, castling, en passant, promotion, check/checkmate/stalemate, algebraic notation, or chess theoretical rules.
- "system": Specifically about the web chess platform features, workflows, creating/joining rooms, WebSocket disconnection/reconnection, matchmaking, timers, user authentication, forum posts, UI, or technical platform issues.
- "all": Touches both domains (e.g. platform timeout during check), is ambiguous, or question_type is "general".

When in doubt, prefer question_type "rag" and domain "all".

Question to classify: {question}

Output ONLY a valid JSON object with keys "question_type" and "domain". No explanation, no markdown backticks.
Example:
{{"question_type": "rag", "domain": "chess"}}
