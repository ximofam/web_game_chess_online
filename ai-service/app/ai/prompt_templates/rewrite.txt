Rewrite and translate the question below into a standalone, concise English search query suitable for a dense vector knowledge base lookup.

Instructions:
1. RESOLVE REFERENCES: Resolve all pronouns and conversational references using the chat history (e.g. "nó", "thế cờ đó", "tính năng đó", "it", "that variation").
2. STANDALONE & CONCISE: Ensure the query is self-contained without needing prior context.
3. TRANSLATE TO ENGLISH: ALWAYS output the final search query in clear, natural English (since the knowledge base and embedding model are optimized for English).
4. CHESS & PLATFORM TERMINOLOGY: Accurately translate domain-specific terms into standard chess and platform vocabulary:
   - "nhập thành" -> "castling" (kingside / queenside)
   - "bắt tốt qua đường" -> "en passant"
   - "phong cấp" -> "pawn promotion"
   - "chiếu / chiếu hết / hòa cờ" -> "check / checkmate / stalemate (draw)"
   - Standard opening names and moves (e.g. "Sicilian Defense", "1.e4 c5")
   - Platform features (e.g. "tạo phòng" -> "create game room", "mất kết nối" -> "WebSocket disconnect / reconnection")
5. OUTPUT FORMAT: Output ONLY the standalone English query. No explanations, no markdown tags, no quotes.

Chat history:
{history}

User question: {question}
